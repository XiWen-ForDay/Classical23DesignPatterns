命令模式

我们来讲命令模式。
这个模式特别适合处理“操作请求”相关的场景，比如你上位机上的按钮点击、设备控制指令等。
它的核心是把每一个操作都封装成一个命令对象，这样一来，你可以轻松实现撤销、重做、任务队列等功能。
比如上位机上有一个“启动设备”的按钮，点击后不是直接调用设备启动的方法，而是创建一个“启动命令”对象，
把这个对象放到命令队列里，再由执行者去执行。
这样做的好处是，你可以随时添加日志记录、权限校验，或者在需要的时候撤销这个操作，非常灵活。