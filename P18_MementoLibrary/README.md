备忘录模式最佳实践示例

豆包示例原型：

备忘录模式，简单说就是在不破坏封装的前提下，捕获一个对象的内部状态，并在该对象之外保存这个状态，
这样以后就可以将对象恢复到之前保存的状态。
比如工控上位机里的配方管理，操作员调整设备参数时，可能需要保存不同版本的配方，或者在调整出错时回滚到上一个可用状态。
用备忘录模式的话，就可以把每个版本的配方参数作为备忘录保存起来，需要的时候直接恢复，不用重新设置，既方便又可靠。