观察者模式

就拿你做上位机时的设备状态监控来说吧，这是观察者模式的经典场景：
比如有一个温度传感器（被观察者），
上位机界面上有实时温度显示面板、温度报警模块、历史数据记录模块（三个观察者）。
当温度传感器检测到温度变化时，不需要主动去通知每个模块，而是让这三个模块“订阅”传感器的状态变化。
一旦温度超过阈值，传感器会自动通知所有订阅者，显示面板更新数值、报警模块触发警报、记录模块保存数据。
这样一来，传感器和各个模块之间解耦了，后续想加一个新的湿度显示模块，直接订阅传感器就行，完全不用改传感器的代码。