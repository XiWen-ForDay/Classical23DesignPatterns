桥接模式最佳实践示例


豆包示例原型：

桥接模式简单说，就是把抽象和实现分开，让它们能独立变化。
比如你做上位机时，可能会遇到这样的情况：有多种设备类型，每种设备又有多种通信方式。
如果用继承，会产生很多组合类，比如SerialModbusDevice、TcpModbusDevice、SerialProfinetDevice等等，非常繁琐。
但用桥接模式，就能把设备类型和通信方式分成两个独立的层次，设备类里持有一个通信接口的引用，
这样想换通信方式，直接换个实现就行，不用修改设备类本身。
