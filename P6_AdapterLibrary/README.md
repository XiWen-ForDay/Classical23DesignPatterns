适配器模式最佳实践示例

豆包示例原型：

适配器模式，简单说就是把一个类的接口转换成客户端期望的另一个接口，让原本不兼容的类可以一起工作。
比如你有个旧的串口温度传感器，它输出的温度数据格式是字符串，像“25.5℃”，但你现在的上位机系统只能处理浮点数类型的温度值。
这时候，你就可以写一个适配器，把传感器输出的字符串格式，转换成系统能接受的浮点数格式。
这样，旧传感器不用修改，就能直接用在新系统里了。