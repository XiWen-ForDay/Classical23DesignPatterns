状态模式最佳实践示例

豆包示例原型：

状态模式简单说，就是当一个对象的内部状态改变时，它的行为也会跟着改变，就像它换了个类一样。
比如工控里的电机，它有运行、停止、故障这几种状态，每种状态下，按启动按钮的反应都不一样：
停止时按启动会运行，运行时按启动可能没反应，故障时按启动则需要先复位。
用状态模式的话，就可以把每种状态的行为封装成单独的类，电机对象持有当前状态的引用，
这样切换状态时，直接换引用就行，不用写一堆if-else判断，代码会更清晰。