责任链模式最佳实践示例


豆包示例原型：

职责链模式，简单说就是把多个处理请求的对象连成一条链，
当有请求过来时，会沿着这条链依次传递，直到某个对象能处理它为止。
比如你做上位机开发时，可能会遇到设备报警的情况，不同级别的报警需要不同的人处理，
像温度过高报警先传给现场操作员，要是没处理再传给班长，还没处理就传给主管，这就是一个典型的职责链。
用这种模式的好处是，不需要提前知道谁能处理这个请求，也不用修改请求发送者的代码，就能灵活地增加或调整处理环节，让代码更符合开闭原则。

